<!DOCTYPE html>
<html>
  <head>
    <title>Rickandmorty</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-dark text-white">
      <a href="<%= root_path %>" class="navbar-brand">WLDD</a>
      <%= form_tag(search_path, :method => "get", id: "search-form", html: {class: 'form-inline'}) do %>
        <div class="row">
            <div class="col-md-8">
            <%= text_field_tag :search, params[:search], placeholder: "Escribe algo ..", class: "form-control" %>
            </div>
            <div class="col-md-4">
            <%= submit_tag "Buscar", :class => 'btn btn-outline-light text-white my-2 my-sm-0 ',:name => nil%>
            </div>
          </div>
      <% end %>
      <!--<%=form_tag search_path, class:"form-inline" , :method => "get" do%>
        <form class="form-inline">
        <input class="form-control mr-sm-2" type="search" placeholder="Buscar" aria-label="Search" action="<%= search_path %>" method="GET">
        <%= label_tag(:search, "Que quieres saber")%>
        <%=button_to "Buscar", search_path, method: "get", class: "btn btn-outline-light text-white my-2 my-sm-0", type: "submit"%>
        <%= text_field_tag :search, params[:search]%>
        <%= submit_tag("Buscar")%>
      <%end%>
      </form>-->
    </nav>
    <%= yield %>

  </body>
</html>
